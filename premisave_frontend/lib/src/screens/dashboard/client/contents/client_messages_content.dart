import 'package:flutter/material.dart';

class ClientMessagesContent extends StatelessWidget {
  const ClientMessagesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Messages Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}